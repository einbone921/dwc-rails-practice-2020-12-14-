<div class="container">
  <div class="row">
    <div class="col-ma-5">
      <%= render "shared/form", book: @book %>
    </div>
    <div class="col-md-7">
      <h1>Users</h1>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan=3></th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
          <tr>
            <td>画像</td>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>